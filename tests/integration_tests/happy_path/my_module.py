import this
import that
from bla import di
from dangle import dongle, Dungle


class MyThingy:
    def __init__(self, a: Dungle) -> None:
        self.a = a

    def do_stuff(self, b) -> str:
        return str(self.a + b)


class MyOtherThingy:
    def tango(self):
        this.do()
        that.dont()
        x = di()
        return dongle(x)
